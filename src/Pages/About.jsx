import React, { useContext } from 'react'
// import { Store } from '../context/Store'

const About = () => {

  // const { currency, product } = useContext(Store)


  return (
    <div>
      About
      {/* {currency}

      {product.map((item, index)=>{
        return(
          <div key={index}>{item.id}</div>
        )
      })}
 */}
    </div>
  )
}

export default About
